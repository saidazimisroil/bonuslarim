<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet" />
    <title>Bonuslarim</title>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Bonuslarim</a>
        <ul class="navbar-nav ms-auto">
          <% if (typeof user !== 'undefined') { %>
          <li class="nav-item">
            <a href="/logout" class="nav-link btn btn-danger">Chiqish</a>
          </li>
          <% } else { %>
          <li class="nav-item">
            <a class="nav-link btn btn-primary" href="/login">Kirish</a>
          </li>
          <li class="nav-item">
            <a class="nav-link btn btn-success" href="/register">Ro'yxatdan o'tish</a>
          </li>
          <% } %>
        </ul>
      </div>
    </nav>
    <div class="container"><%= body %></div>
  </body>
</html>
